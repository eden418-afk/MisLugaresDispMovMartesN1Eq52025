<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="?android:listPreferredItemHeight"
    android:padding="8dp">

    <!-- Foto cuadrada -->
    <ImageView
        android:id="@+id/foto"
        android:layout_width="64dp"
        android:layout_height="0dp"
        android:src="@android:drawable/ic_menu_mapmode"
        android:contentDescription="descripcion foto"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Distancia a la derecha, alineada con el nombre -->
    <TextView
        android:id="@+id/distancia"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="... Km"
        android:textAlignment="viewEnd"
        app:layout_constraintTop_toTopOf="@+id/nombre"
        app:layout_constraintBottom_toBottomOf="@+id/nombre"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Nombre ocupa hasta antes de distancia -->
    <TextView
        android:id="@+id/nombre"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Nombre del lugar"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:paddingStart="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/foto"
        app:layout_constraintEnd_toStartOf="@id/distancia"
        app:layout_goneMarginEnd="8dp" />

    <!-- Dirección debajo del nombre -->
    <TextView
        android:id="@+id/direccion"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Dirección del lugar"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintTop_toBottomOf="@id/nombre"
        app:layout_constraintStart_toStartOf="@id/nombre"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Estrellas debajo de la dirección -->
    <RatingBar
        android:id="@+id/valoracion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:isIndicator="true"
        android:numStars="5"
        android:stepSize="1.0"
        android:rating="3"
        app:layout_constraintTop_toBottomOf="@id/direccion"
        app:layout_constraintStart_toStartOf="@id/nombre"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
